import { LeftOfForwardSlashPipe } from './left-of-forward-slash.pipe';

describe('LeftOfForwardSlashPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftOfForwardSlashPipe();
    expect(pipe).toBeTruthy();
  });
});
