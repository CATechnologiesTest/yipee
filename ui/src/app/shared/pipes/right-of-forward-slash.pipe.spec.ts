import { RightOfForwardSlashPipe } from './right-of-forward-slash.pipe';

describe('RightOfForwardSlashPipe', () => {
  it('create an instance', () => {
    const pipe = new RightOfForwardSlashPipe();
    expect(pipe).toBeTruthy();
  });
});
