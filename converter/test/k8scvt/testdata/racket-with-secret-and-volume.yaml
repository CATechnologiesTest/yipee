# Generated by Yipee.io
# Application: racket-with-secret-and-volume

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaalyume-claim
spec:
  volumeName: vaalyume
  volumeMode: Block
  accessModes:
    - ReadWriteOnce
  storageClassName: bongorondo
  resources:
    requests:
      storage: 1000000

---
apiVersion: v1
kind: Service
metadata:
  name: racket
spec:
  selector:
    name: racket-with-secret-and-volume
    component: racket
  ports:
  - name: racket-80
    targetPort: 80
    port: 80
    protocol: TCP

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: racket
spec:
  selector:
    matchLabels:
      name: racket-with-secret-and-volume
      component: racket
  rollbackTo:
    revision: 0
  template:
    spec:
      containers:
      - name: racket
        image: jackfirth/racket:6.5-onbuild-test
        ports:
        - containerPort: 80
          protocol: TCP
        volumeMounts:
        - mountPath: /mnt/run
          name: secrets
          readOnly: true
        - mountPath: /anbean
          name: vaalyume
          subPath: stuff
      restartPolicy: Always
      volumes:
      - name: secrets
        secret:
          items:
          - key: Secret1
            mode: 292
            path: secrets/SikhRhett
          secretName: secret
      - name: vaalyume
        persistentVolumeClaim:
          claimName: vaalyume-claim
    metadata:
      labels:
        name: racket-with-secret-and-volume
        component: racket
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  replicas: 1
  revisionHistoryLimit: 2

