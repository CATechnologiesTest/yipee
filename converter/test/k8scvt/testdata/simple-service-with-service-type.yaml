# Generated by Yipee.io
# Application: simple-service-with-service-type

apiVersion: v1
kind: Service
metadata:
  name: fff
spec:
  selector:
    a: b
  ports:
  - name: foo
    targetPort: 222
    port: 333
    protocol: TCP
  type: NodePort

---

apiVersion: v1
kind: Service
metadata:
  name: ggg
spec:
  externalName: compose.keeps.jacking.with.my.portals.com
  type: ExternalName

---

apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: ew
spec:
  updateStrategy:
    type: RollingUpdate
  template:
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: ew
        image: foo/bar:baz
        ports:
        - containerPort: 222
          protocol: TCP
          name: foo
        imagePullPolicy: always
    metadata:
      labels:
        name: goof
        component: ew
        a: b
  selector:
    matchLabels:
      name: goof
      component: ew
      a: b
  podManagementPolicy: OrderedReady
  replicas: 1
  serviceName: w

